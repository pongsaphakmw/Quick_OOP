# Write a program by following these requirements:
# 1. Create function name "find_area" that takes 3 parameters: "shape" and "width" and "height" (default value is 1)
# 2. If shape is "circle", calculate the area of the circle and return the result
# 3. If shape is "square", calculate the area of the square and return the result
# 4. If shape is "rectangle", calculate the area of the rectangle and return the result
# 5. If shape is "triangle", calculate the area of the triangle and return the result
# 6. Otherwise, return "Invalid shape"
# 7. Print the result of the function
# 8. Call the function with different parameters and print the result
# 9. Try to handle the letter case of the shape parameter

def find_area():
    pass


if __name__ == '__main__':
    print('Circle area:', find_area('circle', 5))
    print('Square area:', find_area('square', 5))
    print('Rectangle area:', find_area('rectangle', 5))
    print('Triangle area:', find_area('triangle', 5))
    print('Pentagon area:', find_area('pentagon', 5))
